#include <stdio.h>

int main() {
    // Declare an integer variable a and initialize it to 10
    int a = 10;
    // Create a second integer variable b and assign it the double of the value of a
    int b = 2 * a;
    // Display the value of b on the screen
    printf("The value of b is: %d", b);
    return 0;
}

